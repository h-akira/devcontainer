" ~/.vim/add.plugin.vimrc — container-local plugin declarations.
" Sourced from dot.vimrc inside the call plug#begin() / plug#end() block when
" this file exists. Add `Plug '...'` lines here. init-vim.sh will not overwrite
" an existing file once placed.
